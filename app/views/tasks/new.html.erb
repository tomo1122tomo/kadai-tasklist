<h1>タスク新規作成ページ</h1>

<%= form_with(model: @task ) do |f| %>
  <%= f.label :conntent, '新規作成' %>
  <%= f.text_field :conntent %>

  <%= f.submit 'タスクを追加' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>